# Legacy (original 3rd-year submission)

These are the original, unmodified scripts kept for provenance. The revamped,
reproducible pipeline lives in `src/glyphcnn/`. The original code trained on raw
pixel text files (`traindata.txt`, etc.) that are no longer available; the new
pipeline reconstructs the dataset from the rendered PNGs instead.
